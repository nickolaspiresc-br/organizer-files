* File Organizer

A simple Python script to automate file management by organizing items into a dedicated directory.

* Tech Stack:

Python 3
Modules: pathlib (for path manipulation), shutil (for file operations).

* How it works:

The script locates the user's "Downloads" folder and creates a main directory to store and organize the files.